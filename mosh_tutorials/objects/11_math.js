const out = document.querySelector(".out");

Math.random(); // generates random number between 0 and 1

Math.round(1.9); // rounds up to nearest integer

Math.max(1, 2, 3, 4, 5);

Math.min(1, 2, 3, 4, 5);