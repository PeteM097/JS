const out = document.querySelector(".out");

let circle = { };
console.log(circle);

// garbage collector runs in the background
// it figures out what variables  are no longer in use
// and automatically de-allocates their memory